apiVersion: v1
kind: ServiceAccount
metadata:
  name: ${{ values.app_name }}-sa
  namespace: ${{ values.namespace }}
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/${{ values.app_name }}-role